import styled from "styled-components";

export const Main = styled.main`
  h1 {
    font-weight: 700;
    font-size: 2rem;
    margin-bottom: 20px;
  }

  h2 {
    font-weight: 700;
    font-size: 1.5rem;
  }

  ol {
    list-style: decimal;
    list-style-position: inside;

    li {
      margin: 10px 0;
    }
  }

  strong {
    font-weight: 700;
  }
`;
